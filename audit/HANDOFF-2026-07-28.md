# Handoff — jornada 2026-07-28 (chateam_jr)

Estado para retomar en frío. Seis commits **en local, sin pushear**.

```
621171f refactor(wbot): extraer resolveTicketContext() de handleMessageInner
b2de148 test(wbot): cubrir la rama de coexistencia Meta en el golden-master
0c2ab02 test(wbot): golden-master de handleMessageInner contra chateam_test
457da4b docs(security): inventario completo de G2 y G3
fab3aab fix(security): el guard de tenant no cubría la API pública (tokenAuth)
0b18aa0 fix(security): autenticar los webhooks entrantes (Meta, CoinGate, MercadoPago)
--- origin/main y origin/checkpoint están en 4f128c5 ---
```

```bash
cd /home/jcromero09/chateam_jr && git push origin main checkpoint/wip-3meses-2026-07-18
```

El push lo bloquea el clasificador de permisos del agente, no git. Hay que correrlo
a mano o autorizar la regla.

---

## ⚠️ BLOQUEANTE DE DEPLOY — variables de entorno

Estos commits **rompen la ingesta si se despliegan sin configurar el entorno**. No es
opcional. Detalle completo en [`docs/WEBHOOK_AUTH.md`](../docs/WEBHOOK_AUTH.md).

| Variable | Si falta | Estado |
|---|---|---|
| `FACEBOOK_APP_SECRET` | **Todos los webhooks de Meta → 403.** Se corta la ingesta de WhatsApp Cloud y páginas FB/IG. Falla en silencio ("dejaron de entrar mensajes"). | No verificable desde el agente (`.env` protegido) |
| `VERIFY_TOKEN` | El handshake de suscripción de Meta se rechaza. Ya no cae a `"whaticket"`. | ídem |
| `MERCADOPAGO_WEBHOOK_SECRET` | Webhook de MercadoPago → 503 | nueva |
| `COINGATE_CALLBACK_TOKEN` | Callback de CoinGate → 503 | nueva |

El backend grita al arrancar si `META_SIGNATURE_MODE=enforce` (el default nuevo) y
falta `FACEBOOK_APP_SECRET` — ver `assertMetaSignatureConfig()`. **Leer ese log en el
primer arranque post-deploy.**

Escotilla si algo sale mal: `META_SIGNATURE_MODE=warn` (vuelve a aceptar firmas
inválidas — es un canal de inyección abierto, solo para un rollout controlado).

---

## Pendientes, por orden de valor

### 1. Terminar la descomposición de `handleMessageInner` (2 de 3)
Es lo que mueve la nota. `handleMessageInner` está en **1.046 L** (venía de ~1.250).

- **`handleMedia()`** y **`dispatchIntegration()`** — no empezadas.
- Método probado y repetible (lo usé para `resolveTicketContext`):
  1. Medir el contrato: para cada local de la fase, contar usos **y asignaciones**
     en el cuerpo restante. No estimar — en la 1/3 el plan decía "~20+" y eran 12.
     Ojo con falsos positivos: `const x` en scope interno que shadowea, y `x=` dentro
     de template strings.
  2. Mover **con script, no a mano** (`/tmp/.../extract.py` como plantilla).
  3. Golden-master + `tsc` acotado a un `tsconfig.check.json` temporal.
  4. Un commit por extracción.
- Correr el golden-master:
  ```bash
  npx jest --config jest.db.config.cjs tests/harness/handleMessage.dbtest.ts --forceExit
  ```
  18 tests, 12 snapshots. Si pasan **sin reescribirse**, la conducta no cambió.

### 2. `TENANT_SCOPE_GUARD_API`: de `observe` a `enforce`
La API pública (`middleware/tokenAuth`: `/api/send` y familia) ya propaga `companyId`
al contexto, pero el guard ahí está en **`observe`**: loguea lo que inyectaría sin
inyectarlo. Arrancó así a propósito para no activar el scope de golpe sobre endpoints
en producción.

Leer los logs `[tenantScope] would inject companyId` con `surface: "api"`. Si no hay
sorpresas, `TENANT_SCOPE_GUARD_API=enforce`. **Hasta entonces la grieta G1 está
mitigada, no cerrada.**

### 3. Idempotencia de los webhooks de pago
Ninguno desduplica por id de evento. Hoy da igual porque **ni CoinGate ni MercadoPago
acreditan saldo** — solo registran. Es requisito antes de conectarlos a créditos.

### 4. Menores
- Sin lista blanca de IPs para Meta/CoinGate (la firma es la defensa; la IP sería
  defensa en profundidad).
- Los 3 services sin `companyId` propio (`ShowDialogChatBotsServices`,
  `ChatService/ListService`, `AIAgentServices/TicketContextService`): hoy no son
  alcanzables con un id de request, pero no tienen guarda propia. Si se tocan,
  scopearlos primero. Ver `docs/SECURITY_DEBT.md`.
- Envelope de API: 6 de ~891 endpoints.
- `tsc --noEmit` completo **no termina** (OOM del heap V8, exit 134, >8 min). Usar
  `tsconfig.check.json` acotado. Y nunca leer `grep -c "error TS"` como "limpio" sin
  comprobar el exit code: sobre salida vacía devuelve 0 igual.

---

## Lo que NO se verificó

Honestidad sobre el alcance de la verificación de esta jornada:

- **Nada se probó contra tráfico real de Meta.** Este entorno no recibe webhooks (cero
  líneas `META WEBHOOK` en los logs). La firma es correcta por construcción y por 20
  tests unitarios, no por observación en vivo.
- **El manifiesto de MercadoPago** (`id:…;request-id:…;ts:…;`) viene de la documentación
  y de consenso de implementaciones, no de un webhook real recibido. Si estuviera mal,
  el endpoint rechazaría todo el tráfico legítimo. Un webhook real de prueba lo confirma
  en 30 segundos.
- **Nada está desplegado.** Seis commits sin pushear.
